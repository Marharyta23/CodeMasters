export const selectItems = (state) => state.updateWater.items;
export const selectIsLoading = (state) => state.updateWater.isLoading;
export const selectIsError = (state) => state.updateWater.isError;
